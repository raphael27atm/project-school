h2.title Criar Responsável
.box-white
  = simple_form_for(@responsible, html: { class: 'form-horizontal' }) do |f|
    = render 'form', f: f
    = f.submit "Salvar", class: "btn btn-success margin-right"
    = link_to "Voltar", :back, class: "btn btn-warning"  